# 观点句抽取细则（收录审 / 审 A）

> 只回答：**这句话要不要进 Benchmark。**  
> 不管检索对不对、不管失真细类。句子必须是公众号**原文**，禁止改写、禁止按论文手补新句。

审 A 只允许：`keep` / `drop` / `merge`（把被切开的原文并回一句）。

---

## 核心口诀

删掉这句，读者对「**这篇论文说了什么**」的理解会不会变？

- 会 → **keep**
- 只会少一个无关知识点 / 刊头信息 → **drop**

---

## 进（keep）

| 角色 | 含义 | 例子 |
|------|------|------|
| `paper_lead` | 导语：本文发现、方法或「首次/核心」等意义评价 | 该研究……首次揭示了黄瓜下位子房的发育机制 |
| `paper_result` | 本文结果、机制、因果、表型、数值 | 敲除 KNAT2-like1 导致子房上位 |
| `paper_method` | 本文方法、材料、实验设计 | 构建了黄瓜敲除突变体 k-1 和 k-2 |
| `paper_conclusion` | 本文结论、机制总括、育种意义 | FIM 中新基因……是下位子房进化的原因 |
| `paper_intro` | **转述论文引言**的科学框架（定义、进化背景、模式植物、研究空白） | 下位子房被认为是被子植物多次独立进化出的关键创新性状 |

引言句即使读起来像科普，只要是在复述**本篇论文 Introduction**，仍 keep。

元信息 + 科学发现绑在同一句 → **整句 keep**，不要因单位名丢掉后半句。

---

## 不进（drop）

| 角色 | 含义 | 例子 |
|------|------|------|
| `publication_meta` | 纯作者、单位、期刊、题名、DOI | 杨学勇团队在 Nature Plants 发表了题为…… |
| `textbook_bg` | 作者另加、论文未写的学科常识 | 本文讲子房，却插入「光合作用把光能转化为化学能」 |
| `discourse` | 纯过渡套话 | 下面我们来看；综上所述（后面没有命题） |
| `caption_heading` | 图注、分节标题 | 图1 黄瓜单性花与子房下位的进化和发育 |
| `fragment` | 无法独立核查的残句 | （2）花托迅速生长 |

总结段与前文**近重复**、且没有「首次 / 新靶点 / 育种」等新断言 → drop。

---

## 漏抽怎么处理

对照公众号全文扫一遍。该进却没进：

1. 先记在审 A 备注（claim 原文 + 位置）
2. **优先改抽取规则后重抽**，不要人工发明句子
3. 只有规则一时改不了、且该句对失真评测很关键时，才把原文整句补进 `claims` 名单（仍必须是原文）

---

## 审 A 记录字段

对 `A001_claims_for_review.json` 每条：

- `review_decision`: 默认已是 `keep`；发现不该进 Benchmark 时改为 `drop` 或 `merge`
- `review_note`: 可选，一句话理由
- 合并时在 `merge_into` 写目标 `claim_id`

审完后运行 `python scripts/export_locked_claims.py` 导出锁定名单，**再**做论文句检索和失真初稿（审 B）。
